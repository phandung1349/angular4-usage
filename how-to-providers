1. to include self-written service

2. if a few services in file.component.ts, we add like this:
@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css'],
  providers: [IpService, DataService]
})

3. if more, we add in app.module.ts in providers:[]


#tag: providers:[]
